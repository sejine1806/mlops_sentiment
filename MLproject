name: Mamba_Project

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      d_model: {type: int, default: 128}
      lr: {type: float, default: 0.005}
    command: "python train_mamba.py --d_model {d_model} --lr {lr}"
